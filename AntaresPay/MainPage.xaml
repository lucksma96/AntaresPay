<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AntaresPay.MainPage">

    <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

        <Image
                Source="antares_logo_l.png"
                BackgroundColor="Transparent"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="Logo Antares" />

        <Label
                Text="Antares Pay"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

        <Label
                Text="Selecione a operação"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Select monetary operation" />

        <CollectionView>
            <CollectionView.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>Pagar</x:String>
                    <x:String>Cobrar</x:String>
                    <x:String>Extrato</x:String>
                </x:Array>
            </CollectionView.ItemsSource>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="5">
                        <Button Text="{Binding .}"></Button>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>
